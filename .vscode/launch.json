{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Task Management API/package.json",
                "localRoot": "${workspaceFolder}/Task Management API"
            }
        }
    ]
}